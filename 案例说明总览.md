# 案例说明总览（微课 01/02 · 扩展 01/02）

> 课程《端侧智能推理系统 MVP》 ｜ 硬件平台：ESP32-S3-EYE ｜ ESP-IDF 5.4.x ｜ ESP-DL / ESP-PPQ

本文件是 4 份「案例说明」的**索引与摘要**，用于快速了解这四个案例各自做了什么、结果如何、踩过哪些坑。

每个案例的**完整说明**（含代码片段、终端输出原文、逐条踩坑记录、验收清单）是对应目录下的 Markdown 文件，见下表。GitHub 上可直接渲染阅读。

> 说明：本文件与这 4 份案例说明均为学习过程新增的补充材料，不属于课程原始提交内容；课程原有的 `README.md` 与脚本文件未做任何改动。

## 一、索引

| # | 案例 | 说明文档 | 一句话 |
|:---:|---|---|---|
| 微课 01 | 训练 sin 模型 | [`microcourses/01_train_sin_model/案例说明_微课01_训练sin模型.html`](microcourses/01_train_sin_model/案例说明_微课01_训练sin模型.md) | 用 PyTorch 训一个 4353 参数的小网络拟合正弦曲线，导出 ONNX |
| 微课 02 | 量化 sin 模型 | [`microcourses/02_quantize_sin_model/案例说明_微课02_量化sin模型.html`](microcourses/02_quantize_sin_model/案例说明_微课02_量化sin模型.md) | 用 ESP-PPQ 把 ONNX 量化成 8 位 `.espdl`，体积降到 42.2% |
| 扩展 01 | 训练猫狗模型 | [`extensions/01_train_catdog_model/案例说明_扩展01_训练猫狗模型.html`](extensions/01_train_catdog_model/案例说明_扩展01_训练猫狗模型.md) | MobileNetV2 从零训 2400 张真实照片，做猫狗二分类 |
| 扩展 02 | 量化猫狗模型 | [`extensions/02_quantize_catdog_model/案例说明_扩展02_量化猫狗模型.html`](extensions/02_quantize_catdog_model/案例说明_扩展02_量化猫狗模型.md) | 量化猫狗模型，`.espdl` 体积降到 26.73%（压缩比 3.74×） |

## 二、核心数字对照

| 项目 | 微课 01（sin） | 微课 02（sin 量化） | 扩展 01（猫狗） | 扩展 02（猫狗量化） |
|---|---|---|---|---|
| 模型 | 3 层全连接，4353 参数 | 同上 | MobileNetV2，约 220 万参数 | 同上 |
| 输入 | 1 个浮点数 | 1 个浮点数 | 1×3×224×224 | 1×3×224×224 |
| 任务 | 回归（拟合曲线） | 回归 | 分类（cat / dog） | 分类 |
| 输入模型体积 | — | 18,244 B | — | 8,875,417 B |
| 输出 `.espdl` | — | 7,696 B | — | 2,372,240 B |
| 压缩后占比 | — | **42.2%**（省 57.8%） | — | **26.73%**（省 73.27%） |
| 精度代价 | val MSE 0.003033 | float 0.00248 → quant 0.00357（**1.44×**） | val_acc 0.6600（3 轮） | 未实测（见第五节） |

## 三、逐案摘要

### 微课 01 · 训练 sin 模型

**做什么**：训练一个三层全连接网络（`1 → 64 → 64 → 1`，4353 个参数）去拟合 `y = sin(x) + 0.05 × randn()`，再导出成 ONNX，作为微课 02 量化的输入。

**关键配置**：样本 1000（80/20 划分 → 训练 800 / 验证 200）｜批大小 32｜Adam，`lr = 1e-4`｜MSELoss｜500 轮（CPU 上约 13 秒）｜随机种子 42｜噪声强度 0.05

**实测结果**

| 运行 | train loss | val loss |
|---|---|---|
| seed 42 ｜ 老师原版脚本 | 0.002467 | 0.002973 |
| seed 42 ｜ 手写复刻版 | 0.002472 | 0.003033 |
| seed 0 ｜ 老师脚本 | 0.002856 | 0.002525 |
| seed 123 ｜ 老师脚本 | 0.002732 | 0.002863 |

附带指标（由 `.pth` 权重重算，验证集 200 条）：MSE 0.003033 ｜ MAE 0.0430 ｜ R² **0.9936** ｜ 在**无噪声** `sin(x)` 上的 MAE 仅 0.0107 —— 说明网络学到的是 sin 的**形状**，而不是背下了噪声。

**产物**：`sin_model.pth` 20,397 B ｜ `sin_model.onnx` 18,244 B ｜ `loss_history.csv` 11,419 B ｜ `loss_curve.png` 49,244 B ｜ `fit_curve.png` 101,718 B

**主要踩坑**：终端搞混导致"找不到环境"；老师 README 里的 bash 命令在 cmd 下几乎全要改写；照 README 装依赖会**悄悄降级 setuptools**；误以为"设了 seed 就一定可复现"；拿 PNG 图片当"结果完全一致"的证据；报错是套娃的（第一层报错不是根因）；数字凭记忆写错四次。

---

### 微课 02 · 量化 sin 模型

**做什么**：用 ESP-PPQ 对 `sin_model.onnx` 做 **PTQ（训练后量化）**，转成 8 位整数模型 `.espdl`。

**关键配置**：`INPUT_SHAPE [1, 1]`｜`TARGET "esp32s3"`｜`NUM_OF_BITS 8`｜`calib_steps 32`（恰好遍历一遍 1000 条数据）｜`DEVICE "cpu"`｜`export_test_values True`｜`shuffle False`（保证校准顺序确定）

**实测结果**

- 精度代价：float MSE 0.00248 → quant MSE 0.00357，即 **1.44×**（误差增加 44%，同量级，属健康范围）
- 体积收益：18,244 B → 7,696 B，**占原来的 42.2%**

**三个产物**：`sin_model.espdl` 7,696 B（真正烧进芯片的）｜`sin_model.json` 9,032 B（量化参数表，机器可读）｜`sin_model.info` 26,647 B（人类可读的量化报告）

与老师提交产物的差异：`.espdl` 差 16 B、`.json` 差 275 B、`.info` 差 388 B。差异来源已定位为三类：**行尾符**（老师是 LF，本机生成是 CRLF）、**hash / dominator 字段**、**变量打印顺序**——不是量化参数不同。

**主要踩坑**：脚本会覆盖老师提交的产物（已先备份到 `outputs_teacher/`）；README 第 3 步的 pip 命令会降级 setuptools；README 的代码解析漏了一个 `[0]`；**★ 老师脚本的 `collate_fn` 契约写错，而且是静默的**——校准集里混进了一个标签值。

> **诚实记录**：正因为坑 4，本课产出的 `.espdl` 与老师产物在量化参数上完全一致，"与老师一致"这个结论成立；但严格说"校准数据正确"这个说法**不成立**。特此记录，不做掩饰。

---

### 扩展 01 · 训练猫狗模型

**做什么**：用 MobileNetV2（约 220 万参数，`weights=None` 从零开始训）在 2400 张真实照片上训练猫狗二分类，并导出 ONNX。

**数据集**：`cats-dogs-data.zip`，55,203,029 字节（约 55 MB）；结构 `cats-dogs-data/{train,valid}/{cat,dog}`，train 1000 猫 + 1000 狗，valid 200 猫 + 200 狗。

**关键配置**：输入 224×224｜ImageNet 均值/标准差归一化｜CrossEntropyLoss｜3 轮｜随机种子 42（注意：脚本只设了 `torch.manual_seed`）

**实测结果（三轮训练日志）**

| epoch | train_loss | val_loss | val_accuracy |
|:---:|---|---|---|
| 1 | 0.670867 | 0.648905 | 0.6525 |
| 2 | 0.657894 | 0.648010 | 0.6175 |
| 3 | 0.645593 | 0.642834 | **0.6600** |

**五个产物**：`catdog_mobilenet_v2.pth` 9,154,251 B ｜ `catdog_mobilenet_v2.onnx` 8,878,502 B ｜ `labels.txt` 10 B（两行 `cat` / `dog`，决定输出顺序）｜ `metrics.csv` 180 B ｜ `loss_curve.png` 43,150 B

**主要踩坑**：`curl -o` 会把已下载的分片**截断成 0 字节**（要改用支持 Range 续传的地址）；两个 conda 环境谁都跑不了这个脚本；matplotlib 装好了却 import 失败（挖出"半残包"）；训练成功但导出 ONNX 报 `AttributeError`；**★ 同一台机器、同一个 seed，跑两次结果不一样**——根因是 cuDNN 卷积反向传播用了 `atomicAdd`，算法选择不确定，`torch.manual_seed()` 只管随机数生成器，管不了 cuDNN 选哪个算法。

**包修复**：`protobuf`、`python-dateutil`、`onnx` 三个受损包全部用 `install_whl.py` 以"解压覆盖"方式修复，**全程零 `pip uninstall`**，每次修复前先在 `%TEMP%` 隔离验证。

---

### 扩展 02 · 量化猫狗模型

**做什么**：把扩展 01 产出的猫狗 ONNX 模型量化成 8 位 `.espdl`。

**关键配置**：`input_shape [1, 3, 224, 224]`｜`target "esp32s3"`｜`num_of_bits 8`｜`calib_steps min(32, len(loader)) = 32`｜`batch_size 8`（共 256 张图参与定标）｜`shuffle False`｜`device "cpu"`｜`export_test_values **False**`（与微课 02 不同，因为输出太大）

**实测结果**

- 体积收益：8,875,417 B → 2,372,240 B，**占原来的 26.73%**（省 73.27%，压缩比 **3.74×**）
- 对比微课 02 的 sin 模型：压缩后占比 42.2% → 26.73%，压缩比 2.37× → 3.74×

**三个产物**：`catdog_mobilenet_v2.espdl` 2,372,240 B ｜ `catdog_mobilenet_v2.json` 297,965 B ｜ `catdog_mobilenet_v2.info` 14,285,290 B（14 MB，逐层列出数值范围）

**模型指纹（归档用）**

| 文件 | 算法 | 值 |
|---|---|---|
| `.espdl` | MD5 | `1dfb2538b82afd51f4439e4bab6df651` |
| `.espdl` | SHA256 | `12b7bb5e6bca3b20a499059b1add50a6cf7ba977ddc1cc` |
| `.onnx` | MD5 | `6a2ad8300ea8d3f394daa4cd92b9779f` |

**验收清单（6 条）**：① ONNX / 校准数据 / 类别顺序来自同一次运行 —— 已核实；② `import esp_ppq` 无报错 —— 通过；③ 记录芯片 / shape / 位宽 / 校准步数 / 依赖版本 —— 已备齐；④ 两行字节数日志 —— 已获取；⑤ 同一测试集量化前后精度对照 —— **未实测**；⑥ 量化报告 + 模型 hash 归档 —— 已归档。

**主要踩坑**：README 说的校准目录根本不存在；`esp32s3` 环境没有 torchvision；README 第 3 步的 pip 命令会降级 setuptools；**★ 老师脚本的 `collate_fn` 契约写错**（一跑就崩）；上游 ONNX 的时间戳可疑——比 `.pth` 晚了 16 分钟（16:04:49 → 16:20:04）。

## 四、四份说明的统一结构

每份说明都按同一套 6 节组织，便于对照阅读：

1. **这个方案到底在做什么** —— 先建立整体印象，对齐名词
2. **怎么实现的（逐步拆解）** —— 关键配置逐项从源码回读，附实际执行的命令与终端输出
3. **实测结果** —— 核心数据、附带指标、产物清单
4. **遇到的问题（真实踩坑记录）** —— 每条坑写清现象、根因、解决方式
5. **这一课收获了什么**
6. **红线遵守说明** —— 见下节

## 五、红线与诚实标注

四份说明共同遵守的几条：

- **老师原始文件全程未改动**。每份说明都附了核对方式与指纹，例如 `sin_model.py` 实测仍为 180 行 / 6691 字节 / MD5 `9D397A182D983EA2C04920031D7E8F43`，`quantize_catdog.py` 仍为 1806 字节 / MD5 `3ac36cd119da40e5595f1b109ce366de`。需要修改时一律**另存为新副本**（如 `quantize_catdog_fixed.py`），与原文件并存。
- **包修复全程零删除**。三个受损包（protobuf、python-dateutil、onnx）与补装的 torchvision，全部用 `install_whl.py` 以"解压覆盖"方式修复，**没有执行过任何 `pip uninstall`**，每次修复前先在 `%TEMP%` 隔离验证。
- **所有数字均来自实跑产物、源码回读或隔离实验**，无一项凭记忆填写。
- **未实测项明确标注**：扩展 02 验收清单第 5 条「同一测试集量化前后精度对照」本次未做（属 README 的拓展挑战项），因此该页**不给出任何精度下降幅度的数字**。微课 02 中关于 `collate_fn` 的问题也在第 6 节做了诚实记录。

## 六、关于说明文档的格式

四份说明均以 **Markdown** 形式放在各自案例目录下，GitHub 打开即可直接渲染阅读，无需下载。

- `microcourses/01_train_sin_model/案例说明_微课01_训练sin模型.md`
- `microcourses/02_quantize_sin_model/案例说明_微课02_量化sin模型.md`
- `extensions/01_train_catdog_model/案例说明_扩展01_训练猫狗模型.md`
- `extensions/02_quantize_catdog_model/案例说明_扩展02_量化猫狗模型.md`

同目录下另保留了排版好的 `.html` 版本（带配色和卡片样式）。GitHub 不会渲染 `.html`、只显示源码，需要看样式效果时把仓库下载到本地、双击打开即可。

---

《端侧智能推理系统 MVP》 ｜ 案例说明总览 ｜ 2026-09-18
